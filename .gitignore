# Rust build artifacts
/target

# Generated static export: the html + main.css are tracked, the wasm and
# asset bundles are large and rebuilt by `cargo run --bin export`.
/export/wasm
/export/assets

node_modules/
*.log
.DS_Store
