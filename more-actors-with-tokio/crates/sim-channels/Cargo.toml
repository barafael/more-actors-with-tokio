[package]
name = "sim-channels"
version = "0.1.0"
edition = "2021"
description = "Sterile blocking (synchronous) channel primitives mirroring tokio's four channel types; compiles to wasm."
license = "MIT OR Apache-2.0"

[dependencies]
